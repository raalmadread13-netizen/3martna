# Domain Layer (mobile)

Business entities and value types shared by screens and use-case hooks —
mirrors the backend domain model.

Sprint 1 keeps this empty by design; entities (User, Building, Apartment,
Invoice, ...) are added together with their feature sprints.
